using UnityEditor;
using UnityEngine;

// Attach this to any GameObject
public class DrawAxis : MonoBehaviour
{
    // Optional: Configure the line length in the inspector
    public float lineLength = 5f;
}
